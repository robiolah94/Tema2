@model IEnumerable<WebApplication2.Models.Bilete>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Spectacol)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.rand)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.numar)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Spectacol)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.rand)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.numar)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BileteiD }) |
            @Html.ActionLink("Details", "Details", new { id=item.BileteiD }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BileteiD })
        </td>
    </tr>
}

</table>
